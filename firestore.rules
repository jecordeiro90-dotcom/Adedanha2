
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access to the entire 'games' collection and its sub-collections.
    // This is suitable for a simple, anonymous game without user authentication.
    match /games/{gameId=**} {
      allow read, write: if true;
    }
  }
}
